package com.sganzerla.repository;

import com.sganzerla.model.InstrutorTipoTransporte;
import org.springframework.data.jpa.repository.JpaRepository;

public interface InstrutorTipoTransporteRepository extends JpaRepository<InstrutorTipoTransporte, Long> {
}
